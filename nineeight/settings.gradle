pluginManagement {
    repositories {
        maven { url = uri("https://repo.spring.io/release") }
        maven { url = uri("https://plugins.gradle.org/m2") }
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'nineeight'
